FROM python:3.12-slim

WORKDIR /app

COPY backend/agent/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY backend/ ./backend/

ENV PYTHONPATH=/app

RUN useradd -m -u 1000 aria
USER aria

CMD ["python", "-m", "backend.agent.agent", "start"]
